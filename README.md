URL:

```http://localhost:8080/api/v1/employees```

Headers:

```Content-Type: application/json```

Body → raw → JSON:
```
{
    "firstname": "John",
    "lastname": "Smith",
    "email": "john@example.com"
}```
